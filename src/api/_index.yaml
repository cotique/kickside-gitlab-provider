version: "1.0"
namespace: cotique.gitlab_provider.api

entries:
  # cotique.gitlab_provider.api:get_status
  - name: get_status
    kind: function.lua
    meta:
      comment: GET /gitlab-provider/status — static module identity for the gitlab-provider page.
    source: file://get_status.lua
    method: handler
    modules:
      - http
      - security

  # cotique.gitlab_provider.api:get_status.endpoint
  - name: get_status.endpoint
    kind: http.endpoint
    meta:
      comment: Status endpoint behind the authenticated API router.
      router: app:api
    method: GET
    func: get_status
    path: /gitlab-provider/status
