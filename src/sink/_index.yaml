version: "1.0"
namespace: acme.starter.sink

entries:
  # acme.starter.sink:write
  - name: write
    kind: function.lua
    meta:
      comment: kickside.data:writable.write — persist the incoming item, log it, acknowledge.
    source: file://write.lua
    modules:
      - logger
    imports:
      repo: acme.starter.persist:repo
    method: write
