services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cotique_test_gitlab_provider
      POSTGRES_USER: kickside
      POSTGRES_PASSWORD: kickside
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kickside -d cotique_test_gitlab_provider"]
      interval: 2s
      timeout: 3s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data
